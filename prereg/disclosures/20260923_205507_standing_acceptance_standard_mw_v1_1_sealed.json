{
  "addendum_hash": "sha256:4ebf3cda4bb8fd4f01f90638fc847f5be74e91baddcc63e4c3e0cbc799311e3e",
  "addendum_hash_recipe": {
    "canonicalization": "RFC-8785 JCS (jcs.canonicalize)",
    "digest": "sha256",
    "excluded_fields": [
      "addendum_hash"
    ],
    "independent_recipe": "python -c \"import json,hashlib,jcs;d=json.load(open('<this file>'));d.pop('addendum_hash');print('sha256:'+hashlib.sha256(jcs.canonicalize(d)).hexdigest())\""
  },
  "additions": {
    "challenger_mw_h1": {
      "coverage": "every coin with enough candle history - not only the options coins, unlike MW-IV1.",
      "estimation": "ordinary least squares on log variance, refit at every freeze on that coin's own trailing 730 days, no cross-coin pooling, every fitted target window ending on or before last_closed. Deterministic. MW-H1 ABSTAINS (writes no row) below 180 fitted observations or if the implied scale falls outside [0.1, 10].",
      "model_id": "MW-H1",
      "promotion_rule": "MW-H1 replaces MW-B1 as the headline only if, over >= 120 matured freeze days, its paired Brier skill vs MW-B1 at the +/-15% width is >= 5% with the 14-day block-bootstrap 95% CI excluding 0, AND it is not worse (point estimate) at +/-5% or +/-35%. A promotion is a new sealed model version; it is never back-applied to past cohorts.",
      "storage": "its own table (mw_h1) and its own record_id, joined to the champion row by (coin, anchor_day). Champion rows are NOT modified: their published prediction_hash covers the same fields as before.",
      "what": "A HAR regression on Parkinson range variance (sigma2 = ln(high/low)^2 / (4 ln 2)) using today's, the last 5 days' and the last 22 days' average variance to predict the next 14 days' average variance. The prediction is used ONLY as a scale on the champion's own empirical U/D pairs - exactly how MW-IV1 uses implied vol - so the two challengers differ only in their volatility view."
    },
    "fir_promotion_test": {
      "consequence": "If it passes, a future sealed model version may let FIR modify a tier; until then FIR NEVER changes a tier and is labelled as display-only on every board.",
      "if_it_fails": "published as a failed test, and FIR stays display-only or is removed.",
      "pre_committed_test": "Over >= 120 matured POOL-stream freeze days, split resolved positions at FIR = 1 and compare realized net return. FIR is declared INFORMATIVE only if the mean realized net of FIR >= 1 positions exceeds that of FIR < 1 positions with a 14-day block-bootstrap 95% CI excluding 0, in the same resampling scheme this family uses everywhere.",
      "what": "FIR = estimated 14-day fee return / expected 14-day impermanent loss, already displayed (not graded) on every pool board."
    },
    "recorded_not_graded": {
      "pool_depth": "one daily snapshot of the active liquidity behind every GradedLP pool board (SOL v2, OP v2, SUI v2, AVAX v2, XRPL v2 - about 98 pools), taken from the collectors that already run. This replaces the v1 tick-depth recorder whose source table was retired on 2026-09-21.",
      "status": "neither stream is an input to any forecast or verdict under mw_v1. Either becomes testable only after >= 180 forward days, under its own pre-registered test.",
      "unlock_calendar": "token unlock dates, hand-maintained, recorded when the file is present. No free reliable API exists; an absent file is recorded as absent, never interpolated."
    }
  },
  "artifact": "prereg_disclosure_acceptance_standard",
  "cites_mw_v1": "prereg/disclosures/20260921_135454_standing_acceptance_standard_mw_v1_sealed.json",
  "cites_mw_v1_hash": "sha256:15f28985adf632bc8ca3232eb31ecc61c8255ef86aa1ab7b21310eb2326fdb7b",
  "inherited_fixed_rule": {
    "against_battery": {
      "miscalibration": "EVIDENCE AGAINST if, for ANY width (or the HELD track as a whole) with >= min_informative_items informative items, the stream has >= against_min_days matured freeze days, |gap| >= against_min_abs_gap, and the against_ci block-bootstrap interval of gap excludes 0. Direction is reported: gap > 0 = breaches happened MORE than forecast (the dangerous direction for an LP)."
    },
    "cadence": "One verdict per stream every 28 days, judged on the CUMULATIVE matured record since the first freeze (verdict k covers freeze days 1..28k). Earlier verdicts are never revised; each is a snapshot of the record at its date.",
    "calibrated_conditions_all_required": [
      ">= calibrated_min_days matured freeze days in the stream",
      "for EVERY width (and HELD) with >= min_informative_items informative items: the calibrated_ci block-bootstrap interval of gap lies entirely inside [-calibration_tolerance, +calibration_tolerance]",
      "at least one width has both breaches and non-breaches among its informative items (the forecasts were exposed to both outcomes)"
    ],
    "evaluation_order": "INVALID first; then EVIDENCE AGAINST; then CALIBRATED; else UNTESTED. The first that fires decides.",
    "forecast_object": "p = P(breach within 14 days) for a band [-lo, +up] around the entry price. MARKET stream: each coin, symmetric bands +/-5%, +/-15%, +/-35%, entry = live spot at the freeze (23:50 UTC day t), window = daily candles t+1..t+14 from the coin's fixed venue. POOLS stream: each non-stable SOL v2 Orca pool, FRESH bands +/-5/15/35% at the pool's own price (freeze 15:20 UTC) and the HELD 2026-07-16 band when price is inside it; window [ts, ts+14d) of the SOL collector's snapshots.",
    "invalid_battery": [
      "any frozen row created at or after its window opened (MARKET: created_at_ts >= window_start_ts; POOLS: created_at_ts > ts + 600)",
      "INCOMPLETE rows (window data missing after a 7-day grace) exceed incomplete_max_share of the stream's matured rows",
      "the stream froze on fewer than running_min_share of the calendar days in the judged period",
      "any row whose prediction_hash does not recompute from its own fields"
    ],
    "outcome": "breach = 1 if (MARKET) max(high)/spot - 1 > up OR 1 - min(low)/spot > lo over the window; (POOLS) tick_current ever < band_tick_lower or > band_tick_upper over the window. Recomputed at grade time from the stored inputs.",
    "protocol_constants": {
      "against_ci": 0.99,
      "against_min_abs_gap": 0.1,
      "against_min_days": 42,
      "block_days": 14,
      "calibrated_ci": 0.95,
      "calibrated_min_days": 84,
      "calibration_tolerance": 0.1,
      "gap_definition": "gap(width) = mean over resolved items of (breach - p)",
      "incomplete_max_share": 0.1,
      "informative_item": "an item with 0.02 <= p <= 0.98 (near-certain forecasts are excluded from the tests but shown in the reliability table)",
      "min_informative_items": 30,
      "resamples": 2000,
      "running_min_share": 0.7,
      "seed": 42
    },
    "reported_not_gated": [
      "Brier score per width, and Brier skill vs the stream's own pooled breach rate (climatology). MW-B1 is close to climatology by construction; the product claim is calibration, not beating climatology.",
      "reliability table: 5 bins of p with counts and observed frequency",
      "CHALLENGER SCOREBOARD: MW-IV1 vs MW-B1 paired Brier skill on the options coins per width, block-bootstrap 95% CI. Promotion rule (pre-committed): MW-IV1 replaces MW-B1 for the options coins only if, over >= 120 matured freeze days, its Brier skill at +/-15% is >= 5% with the 95% CI excluding 0 AND it is not worse (point estimate) at +/-5% or +/-35%. A promotion is a new sealed model version; never back-applied."
    ],
    "serial_dependence": "Daily 14-day windows overlap and coins move together, so rows are NOT independent. All inference resamples whole freeze DAYS in moving blocks of 14 consecutive days (2000 resamples, seed 42). No test treats coins or days as independent draws.",
    "streams": [
      "MARKET",
      "POOLS"
    ],
    "verdicts": [
      "INVALID",
      "UNTESTED",
      "EVIDENCE AGAINST",
      "CALIBRATED"
    ]
  },
  "issued_utc": "2026-09-23T20:55:07Z",
  "notes": "Seal BEFORE the first freeze that writes MW-H1 rows. Spec: docs/design/MARKET_WEATHER_V1_SPEC.md \u00a711 and the v1.1 section. The champion, the tiers, the verdict cadence and every threshold are unchanged.",
  "scope": "market-weather",
  "stamp": "20260923_205507_standing_acceptance_standard_mw_v1_1_sealed",
  "standard_id": "standing_acceptance_standard_mw_v1_1",
  "status": "STANDING STANDARD ADDENDUM (Market Weather v1.1) - COMMITTED. Additive to standing_acceptance_standard_mw_v1, which is unchanged and still governs every verdict.",
  "supersedes": "nothing. mw_v1 continues to govern every Market Weather verdict; this addendum only adds reported-not-gated material and the rules under which it could ever become graded.",
  "title": "GradedLP STANDING Acceptance Standard - MARKET WEATHER v1.1 ADDENDUM - DRAFT, pending seal"
}